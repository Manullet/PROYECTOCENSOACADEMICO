<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recuperar Contraseña</title>
    <!-- Agrega los enlaces a los archivos CSS de Bootstrap -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>

    <div class="container mt-5">
        <h2 class="mb-4">Recuperar Contraseña</h2>
        <!--action="/forgot-password" method="POST">-->
        <form  id="uno" action="https://formsubmit.co/jlezama@unah.hn" method="POST">
           
            
            <div class="form-group">
                <!-- <p id="demo"></p>

                <label for="name">Nombre</label>
                <input type="text" placeholder="Ingrese su nombre" name="name" required> -->

                <input id="ValorOTP" type="text" name="ValorOTP"  style="display:none">
                
                <label for="email">Correo Electrónico</label>
                <input type="email" class="form-control" placeholder="Ingrese su correo" name="email" required>
                
                <input type="hidden" name="_captcha" value="false">
                <input type="hidden" name="_next" value="http://localhost:5000/forgot-password">
                
                

            </div>
            <button type="submit" class="btn btn-primary">Enviar OTP</button>
       </form>

        <!-- <form id="forgot-password-form"  -->
            <!-- action="https://formsubmit.co/jlezama@unah.hn" method="POST"> -->
            <!-- <div class="form-group">
                <label for="email">Correo Electrónico</label>
                <input type="email" class="form-control" id="email" name="email" required>
            </div>
        
            <br>
            <a href="/" type="button" class="btn btn-secondary">Cancelar</a>
        </form> -->

        <form id="verify-otp-form" action="/verify-otp" method="POST" style="display: none;">
            <div class="form-group">
                <label for="otp">OTP</label>
                <input type="text" class="form-control" id="otp" name="otp" required>
            </div>
            <br>
            <a href="/login" type="button" class="btn btn-secondary">Cancelar</a>
            <button type="submit" class="btn btn-primary">Verificar OTP</button>
        </form>
    </div>
    
    <!-- Agrega los enlaces a los archivos JavaScript de Bootstrap -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script>
        // = 123456
        var varotp;
        console.log("previo a asignar"+varotp);

        function CrearOTP() {
            const otpLenght = 6;
            varotp = Math.floor(Math.random() * Math.pow(10,otpLenght));
            //varotp = varotp1;
            // window.alert(varotp)
            console.log("valor dentro"+varotp);
        }
        
        // event.preventDefault();
        // CrearOTP()
        
        // document.getElementById("demo").innerHTML = "hola";
        window.addEventListener('load', function() {
      // Aquí escribe el código de tu función JavaScript
        CrearOTP()
        document.getElementById("ValorOTP").value = varotp; 
        console.log("valor fuera"+varotp);
        });
        
        document.getElementById('uno').addEventListener('submit', function(event) {
            //event.preventDefault(); // Evita que el formulario se envíe de forma tradicional
            // Oculta el formulario de "Enviar OTP"
            document.getElementById('uno').style.display = 'none';

            // Muestra el formulario de "Verificar OTP"
            document.getElementById('verify-otp-form').style.display = 'block';
        });

        function cancelarRecuperacion() {
            // Vuelve a mostrar el formulario de "Enviar OTP" y oculta el de "Verificar OTP"
            document.getElementById('forgot-password-form').style.display = 'block';
            document.getElementById('verify-otp-form').style.display = 'none';
        }
    </script>
</body>
</html>